apiVersion: v1
kind: Service
metadata:
  name: nginx-clusterip
  labels:
    app: nginx-lab
spec:
  type: ClusterIP
  selector:
    app: nginx-lab
  ports:
    - name: http
      port: 80
      targetPort: 80

